# Graduation Invitation

An interactive graduation party invitation website with advanced visual capabilities using MCP integration.

## Setup
1. Install dependencies: `npm install`
2. Run development server: `npm run dev`
3. Build for production: `npm run build`

## MCP Integration
This project uses Figma and Canva MCP servers for design generation and visual assets.
