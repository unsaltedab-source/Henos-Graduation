name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
      - feat/graduation-invitation

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Install and Build 🔧
        run: |
          cd graduation-invitation
          npm install
          npm run build

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: graduation-invitation/dist
          branch: gh-pages
